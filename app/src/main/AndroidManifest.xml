<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icono_pcr_negro_transparente"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icono_pcr_negro_transparente"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProCookingRecipes"
        tools:targetApi="31">

        <activity
            android:name=".ui.Activity.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.ProCookingRecipes.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.Activity.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ProCookingRecipes.NoActionBar">
        </activity>
        <activity
            android:name=".ui.Activity.BusquedaActivity"
            android:exported="true"
            android:theme="@style/Theme.ProCookingRecipes.NoActionBar">
        </activity>
        <activity
            android:name=".ui.Activity.RecetaActivity"
            android:exported="true">
        </activity>
    </application>
</manifest>